[Unit]
Description=Bridge CRM sandbox gunicorn service
After=network.target postgresql.service

[Service]
User=crm-app
Group=crm-shared
WorkingDirectory=/opt/crm-runtime
Environment=PYTHONPATH=/opt/crm-runtime
EnvironmentFile=/home/ubuntu/bridge_crm_sandbox/.env
ExecStart=/home/ubuntu/crm-env/bin/gunicorn \
  --workers 2 \
  --bind 127.0.0.1:5002 \
  --access-logfile - \
  --error-logfile - \
  bridge_crm.wsgi:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
